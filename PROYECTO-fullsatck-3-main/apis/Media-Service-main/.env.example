# URL publica desde la que el navegador carga las imagenes servidas por MediaService.
# En dev apunta al puerto local del servicio. En prod, al dominio/CDN publico.
MEDIA_PUBLIC_URL=http://localhost:8006
